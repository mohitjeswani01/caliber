"""Test package for Caliber.

Required coverage (per CLAUDE.md): schema loading, the honeypot detector, the
ranking metrics (NDCG/MAP/P@k), and CSV submission format. ``validate_submission.py``
(the bundled format validator) lives alongside these tests.
"""
